import { NetlessApp } from "@netless/window-manager";
import styles from "./style.scss";

const NetlessAppMediaPlayer: NetlessApp<unknown> = {
    kind: "MediaPlayer",
    setup(context) {},
};

export default NetlessAppMediaPlayer;
